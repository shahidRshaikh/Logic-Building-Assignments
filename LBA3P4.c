// Accept one character from uder and convert case of that character

#include<stdio.h>

void DisplayConvert(char cAlphabate)
{
    if(()) 
    {
        printf("%c\n",);
    }
    else if(())
    {
        printf("%c\n",);
    }
}

int main()
{
    char cValue = '\0';

    printf("Enter the Character : \n");
    scanf("%c",&cValue);

    DisplayConvert(cValue);

    return 0;
}